# SOFTWARE ARCHITECTURE & SYSTEM DESIGN
## Sistem Deteksi Tanaman Melon Siap Pangkas (ESP32-CAM + AI)

---

# TAHAP 1 — CONTEXT DIAGRAM

Context diagram menggambarkan batas sistem (system boundary) dan interaksi level tertinggi antara sistem dengan entitas eksternal.

```mermaid
flowchart TB
    User([User / Peneliti])
    Admin([Administrator])
    ESP([ESP32-CAM])

    subgraph SYS["SISTEM DETEKSI TANAMAN MELON"]
        API[FastAPI Backend]
        AI[Model AI - MobileNetV2 FOMO TFLite]
        DB[(Database)]
        FE[Dashboard NextJS]
    end

    ESP -->|HTTP POST: gambar periodik| API
    API -->|gambar mentah| AI
    AI -->|bounding box + label + status| API
    API -->|simpan hasil deteksi| DB
    DB -->|riwayat & statistik| API
    API -->|REST API JSON| FE
    FE -->|tampilan dashboard| User
    Admin -->|kelola user, kelola aturan status| API
    User -->|login, lihat history, download laporan| FE
```

**Penjelasan relasi:**
- **ESP32-CAM** adalah satu-satunya sumber data citra; berkomunikasi searah ke backend via HTTP POST.
- **FastAPI Backend** menjadi orkestrator tunggal: menerima gambar, memanggil Model AI, menyimpan ke Database, dan menyajikan data ke Frontend.
- **Model AI** adalah komponen internal yang dipanggil backend secara sinkron, bukan entitas eksternal yang berdiri sendiri secara jaringan.
- **Administrator** berinteraksi dengan sistem untuk provisioning user dan konfigurasi aturan status pangkas (bukan melalui registrasi publik).
- **User** hanya berinteraksi melalui Dashboard (read-only terhadap proses AI, tidak bisa memicu inferensi manual selain melalui mekanisme yang disediakan).

---

# TAHAP 2 — USE CASE DIAGRAM

```mermaid
flowchart LR
    Admin([Administrator])
    User([User])
    ESP([ESP32-CAM])
    AIModel([Model AI])
    DB([Database])

    subgraph UC["Use Case - Sistem Deteksi Melon"]
        UC1((Login))
        UC2((Upload Image))
        UC3((Jalankan Inferensi))
        UC4((Lihat Live Detection))
        UC5((Lihat History Deteksi))
        UC6((Lihat Detail Deteksi))
        UC7((Lihat Statistik))
        UC8((Lihat Status Sistem))
        UC9((Download Laporan))
        UC10((Kelola User))
        UC11((Kelola Aturan Status Pangkas))
        UC12((Simpan Hasil Deteksi))
        UC13((Generate Bounding Box))
    end

    ESP --> UC2
    UC2 --> UC3
    UC3 --> UC13
    AIModel --> UC13
    UC13 --> UC12
    UC12 --> DB

    User --> UC1
    User --> UC4
    User --> UC5
    User --> UC6
    User --> UC7
    User --> UC8
    User --> UC9

    Admin --> UC1
    Admin --> UC10
    Admin --> UC11
    Admin --> UC8

    UC5 -.include.-> UC6
    UC3 -.include.-> UC12
```

**Catatan use case penting:**
- *Upload Image* hanya dilakukan oleh ESP32-CAM (bukan user) — sesuai requirement, sistem tidak mendukung upload manual oleh user.
- *Jalankan Inferensi* dan *Generate Bounding Box* bersifat otomatis (triggered event), bukan use case yang diinisiasi user.
- *Kelola Aturan Status Pangkas* khusus Administrator, mendukung requirement "aturan harus mudah diubah".
- *Download Laporan* saat ini hanya format TXT, namun use case dirancang generik (`Download Report`) agar dapat diperluas tanpa mengubah relasi use case.

---

# TAHAP 3 — ACTIVITY DIAGRAM

### 3.1 Login

```mermaid
flowchart TD
    Start([Mulai]) --> A[User membuka halaman login]
    A --> B[Input username & password]
    B --> C{Kredensial valid?}
    C -->|Tidak| D[Tampilkan pesan error]
    D --> B
    C -->|Ya| E[Generate session/token]
    E --> F[Redirect ke Dashboard]
    F --> End([Selesai])
```

### 3.2 Upload Image (oleh ESP32-CAM)

```mermaid
flowchart TD
    Start([Mulai]) --> A[ESP32-CAM capture gambar]
    A --> B[Encode gambar]
    B --> C[Kirim HTTP POST ke /upload]
    C --> D{Backend menerima?}
    D -->|Gagal| E[Retry / log error]
    E --> C
    D -->|Berhasil| F[Simpan gambar sementara]
    F --> G[Trigger proses /predict]
    G --> End([Selesai])
```

### 3.3 Realtime Detection

```mermaid
flowchart TD
    Start([Mulai]) --> A[Backend menerima gambar baru]
    A --> B[Preprocessing gambar - OpenCV]
    B --> C[Inferensi model TFLite MobileNetV2 FOMO]
    C --> D[Hasil: koordinat bounding box + label]
    D --> E[Hitung status pangkas berdasarkan aturan]
    E --> F[Gambar bounding box ke citra]
    F --> G[Simpan hasil ke Database]
    G --> H[Update endpoint /latest dan /status]
    H --> I[Frontend polling/refresh otomatis]
    I --> J[Tampilkan hasil terbaru ke User]
    J --> End([Selesai])
```

### 3.4 History

```mermaid
flowchart TD
    Start([Mulai]) --> A[User membuka halaman History]
    A --> B[Frontend request GET /history]
    B --> C[Backend query Database dengan filter/pagination]
    C --> D[Return daftar deteksi]
    D --> E[User pilih salah satu item]
    E --> F[Frontend request GET /history/id]
    F --> G[Tampilkan detail: gambar bounding box, label, status, durasi inferensi]
    G --> End([Selesai])
```

### 3.5 Monitoring

```mermaid
flowchart TD
    Start([Mulai]) --> A[Dashboard request GET /status secara berkala]
    A --> B[Backend cek status ESP32 - timestamp gambar terakhir]
    B --> C[Backend cek status koneksi Database]
    C --> D[Backend cek status Model AI - berhasil dimuat]
    D --> E[Backend cek status resource VPS]
    E --> F[Return status agregat]
    F --> G[Frontend update indikator status & uptime]
    G --> End([Selesai])
```

### 3.6 Download Report

```mermaid
flowchart TD
    Start([Mulai]) --> A[User memilih rentang data / filter]
    A --> B[Frontend request GET /download]
    B --> C[Backend ambil data dari Database sesuai filter]
    C --> D[Backend generate file - format TXT saat ini]
    D --> E{Format didukung?}
    E -->|Tidak| F[Tampilkan pesan format belum tersedia]
    E -->|Ya| G[Kirim file ke browser]
    G --> H[User mengunduh file]
    H --> End([Selesai])
```

---

# TAHAP 4 — SEQUENCE DIAGRAM

```mermaid
sequenceDiagram
    participant ESP as ESP32-CAM
    participant API as FastAPI Backend
    participant CV as OpenCV (Preprocessing)
    participant AI as TFLite Model (MobileNetV2 FOMO)
    participant DB as Database
    participant FE as NextJS Dashboard
    participant U as User

    ESP->>API: POST /upload (gambar)
    API->>API: Validasi & simpan file sementara
    API->>CV: Preprocess gambar
    CV-->>API: Gambar siap inferensi
    API->>AI: Jalankan inferensi (predict)
    AI-->>API: Bounding box + label (+confidence internal)
    API->>API: Tentukan status pangkas (rule engine)
    API->>CV: Gambar bounding box ke citra
    CV-->>API: Citra hasil anotasi
    API->>DB: INSERT hasil deteksi (tanpa confidence)
    DB-->>API: Konfirmasi tersimpan
    API-->>ESP: Response 200 OK

    FE->>API: GET /latest (polling berkala)
    API->>DB: Query data terbaru
    DB-->>API: Data deteksi terbaru
    API-->>FE: JSON response
    FE-->>U: Render hasil deteksi realtime
```

---

# TAHAP 5 — COMPONENT DIAGRAM

```mermaid
flowchart TB
    subgraph FRONTEND["Frontend - NextJS + Tailwind"]
        F1[Auth Module]
        F2[Dashboard Module]
        F3[History Module]
        F4[Statistics Module]
        F5[Monitoring Module]
        F6[Download Module]
    end

    subgraph BACKEND["Backend - FastAPI"]
        B1[Auth Service]
        B2[Upload Handler]
        B3[Detection Service]
        B4[History Service]
        B5[Statistics Service]
        B6[Monitoring Service]
        B7[Export Service]
        B8[Status Rule Engine]
    end

    subgraph AIENGINE["AI Engine"]
        A1[Model Loader - TFLite]
        A2[Preprocessing - OpenCV]
        A3[Inference Runner]
        A4[Bounding Box Generator]
    end

    subgraph DATA["Database Layer"]
        D1[SQLAlchemy ORM]
        D2[(PostgreSQL / MySQL)]
    end

    subgraph HW["Hardware Communication"]
        H1[ESP32-CAM HTTP Client]
    end

    subgraph STORAGE["Storage"]
        S1[Image File Storage]
        S2[Export File Storage]
    end

    H1 -->|HTTP POST| B2
    F1 --> B1
    F2 --> B3
    F2 --> B6
    F3 --> B4
    F4 --> B5
    F5 --> B6
    F6 --> B7

    B2 --> A2
    A2 --> A3
    A3 --> A1
    A3 --> A4
    A4 --> B8
    B8 --> B3
    B3 --> D1
    B4 --> D1
    B5 --> D1
    B7 --> D1
    D1 --> D2
    B2 --> S1
    B7 --> S2
```

---

# TAHAP 6 — DEPLOYMENT DIAGRAM

```mermaid
flowchart TB
    subgraph CLIENT["Client Device"]
        Laptop[Laptop / Desktop Browser]
    end

    subgraph IOT["IoT Device"]
        ESP[ESP32-CAM AI Thinker]
    end

    subgraph VPS["Ubuntu Server 22.04 LTS - VPS - 1vCPU/4GB RAM/50GB NVMe"]
        subgraph WEBSRV["Web Server Layer"]
            Nginx[Nginx Reverse Proxy]
        end
        subgraph APPSRV["Application Server"]
            FastAPI[FastAPI - Uvicorn/Gunicorn]
            TFLite[TensorFlow Lite Runtime]
        end
        subgraph FESRV["Frontend Server"]
            NextJS[NextJS App - Node Process]
        end
        subgraph DBSRV["Database Server"]
            PG[(PostgreSQL)]
        end
        subgraph FS["Filesystem"]
            Files[/Image & Export Storage/]
        end
    end

    ESP -->|HTTPS POST| Nginx
    Nginx --> FastAPI
    FastAPI --> TFLite
    FastAPI --> PG
    FastAPI --> Files
    Nginx --> NextJS
    Laptop -->|HTTPS| Nginx
```

**Catatan deployment:**
- Nginx berfungsi sebagai reverse proxy tunggal yang merutekan trafik ke FastAPI (API) dan NextJS (frontend), sekaligus menangani SSL termination.
- Mengingat spesifikasi VPS terbatas (1 vCPU/4GB RAM), TensorFlow Lite dijalankan dalam proses yang sama dengan FastAPI (in-process inference) untuk menghindari overhead komunikasi antar-proses/container terpisah.
- PostgreSQL dijalankan sebagai service terpisah pada VPS yang sama; konfigurasi koneksi dipisahkan agar dapat dipindah ke MySQL atau instance terpisah tanpa mengubah kode aplikasi.

---

# TAHAP 7 — CLEAN ARCHITECTURE

```mermaid
flowchart TB
    subgraph L1["Presentation Layer"]
        P1[NextJS Pages/Components]
        P2[Tailwind UI]
    end

    subgraph L2["API Layer"]
        API1[FastAPI Routers: auth, upload, predict, history, statistics, status, download]
        API2[Request/Response Schemas - Pydantic]
        API3[Middleware - Auth, CORS, Error Handler]
    end

    subgraph L3["Service Layer"]
        S1[AuthService]
        S2[DetectionService]
        S3[HistoryService]
        S4[StatisticsService]
        S5[MonitoringService]
        S6[ExportService]
        S7[StatusRuleService]
    end

    subgraph L4["Repository Layer"]
        R1[UserRepository]
        R2[DetectionRepository]
        R3[DeviceRepository]
    end

    subgraph L5["Database Layer"]
        DB1[SQLAlchemy Models]
        DB2[(PostgreSQL/MySQL)]
    end

    subgraph L6["AI Layer"]
        AI1[ModelLoader]
        AI2[Preprocessor]
        AI3[InferenceEngine]
        AI4[BoundingBoxRenderer]
    end

    subgraph L7["Hardware Layer"]
        HW1[ESP32 Communication Adapter]
    end

    subgraph L8["Configuration Layer"]
        C1[Env Config]
        C2[Database Config]
        C3[Model Config]
        C4[Status Rule Config]
    end

    subgraph L9["Utility Layer"]
        U1[Logger]
        U2[File Handler]
        U3[Response Formatter]
        U4[Exception Handler]
    end

    L1 --> L2
    L2 --> L3
    L3 --> L4
    L4 --> L5
    L3 --> L6
    L7 --> L2
    L8 -.config.-> L2
    L8 -.config.-> L3
    L8 -.config.-> L5
    L8 -.config.-> L6
    L9 -.dipakai semua layer.-> L2
    L9 -.dipakai semua layer.-> L3
```

**Prinsip dependency direction:**
- Dependensi mengalir satu arah: Presentation → API → Service → Repository → Database.
- Layer **AI** dan **Hardware** bersifat *pluggable*: Service Layer berkomunikasi dengan AI Layer melalui interface/abstraksi, sehingga model TFLite dapat diganti tanpa mengubah Service Layer.
- Layer **Configuration** tidak bergantung pada layer lain manapun (paling independen), dibaca oleh seluruh layer yang membutuhkan.
- Layer **Utility** bersifat lintas-layer (cross-cutting concern: logging, error handling) dan tidak boleh memiliki dependensi balik ke business logic.
- Repository Layer mengisolasi Service Layer dari detail ORM/SQL, sehingga migrasi PostgreSQL → MySQL hanya menyentuh Database Layer & Configuration Layer.

---

# TAHAP 8 — RANCANGAN KOMUNIKASI DATA

```mermaid
sequenceDiagram
    participant ESP as ESP32-CAM
    participant HTTP as HTTP POST
    participant API as FastAPI
    participant TFL as TensorFlow Lite
    participant CV as OpenCV
    participant DB as Database
    participant REST as REST API
    participant NX as NextJS
    participant DASH as Dashboard

    ESP->>HTTP: Kirim payload gambar (binary/multipart)
    HTTP->>API: Terima request di endpoint /upload
    API->>TFL: Forward citra terproses untuk inferensi
    TFL->>CV: Hasil mentah diteruskan untuk anotasi visual
    CV->>DB: Simpan record: label, status, bbox image, durasi
    DB->>REST: Sediakan data melalui query layer
    REST->>NX: Response JSON (GET /latest, /history, /statistics)
    NX->>DASH: Render komponen UI (grafik, tabel, status)
```

**Format payload (konseptual, bukan kode):**

| Tahap | Format Data |
|---|---|
| ESP32 → FastAPI | `multipart/form-data` berisi binary image + device_id |
| FastAPI → TFLite | Array tensor hasil preprocessing (resize, normalisasi) |
| TFLite → OpenCV | Koordinat bounding box + label index |
| FastAPI → Database | Record terstruktur (JSON-serializable, sesuai skema History) |
| FastAPI → NextJS | `application/json` REST response |
| NextJS → Dashboard | Props/state React untuk rendering komponen |

**Mekanisme realtime (tanpa streaming):**
Frontend melakukan *short-interval polling* (mis. setiap beberapa detik) terhadap endpoint `/latest` dan `/status`, bukan koneksi persisten (WebSocket/SSE), sesuai requirement bahwa sistem tidak menggunakan video streaming.

---

# TAHAP 9 — STRUKTUR FOLDER PROJECT

```
deteksi-melon/
│
├── frontend/                      # NextJS + Tailwind + TypeScript
│   ├── src/
│   │   ├── app/                   # Routing (App Router)
│   │   ├── components/            # UI Components
│   │   ├── modules/                # Dashboard, History, Statistics, Monitoring, Auth
│   │   ├── services/                # API client (fetch wrapper)
│   │   ├── types/                   # TypeScript interfaces
│   │   └── styles/                  # Tailwind config & global styles
│   ├── public/
│   └── package.json
│
├── backend/                       # FastAPI
│   ├── app/
│   │   ├── api/                     # Routers: auth, upload, predict, history, statistics, status, download
│   │   ├── services/                # Business logic layer
│   │   ├── repositories/            # Data access layer
│   │   ├── schemas/                 # Pydantic models
│   │   ├── core/                    # Config, security, middleware
│   │   ├── utils/                   # Logger, file handler, formatter
│   │   └── hardware/                # ESP32 communication adapter
│   ├── tests/
│   └── requirements.txt
│
├── ai-model/                      # Terpisah agar mudah diganti
│   ├── models/
│   │   └── mobilenetv2_fomo.lite
│   ├── inference/                  # ModelLoader, Preprocessor, InferenceEngine
│   ├── labels.yaml                 # Definisi label & status rule
│   └── version.txt
│
├── database/
│   ├── migrations/                 # Alembic migration scripts
│   ├── models/                     # SQLAlchemy ORM models
│   └── seeders/                    # Data awal (admin user, dsb.)
│
├── deployment/
│   ├── nginx/
│   ├── systemd/                    # Service files FastAPI & NextJS
│   ├── scripts/                    # Backup, deploy, restart scripts
│   └── env/                        # .env templates per environment
│
├── documentation/
│   ├── architecture/               # Diagram-diagram desain (file ini)
│   ├── api-spec/                   # OpenAPI/Swagger export
│   └── srs/                        # Software Requirement Specification
│
└── assets/
    ├── sample-images/
    └── design-references/
```

**Rasional pemisahan folder:**
- `ai-model/` dipisah total dari `backend/` agar model dapat diperbarui (versi baru hasil training ulang) tanpa menyentuh kode aplikasi — backend cukup membaca path model dari konfigurasi.
- `database/migrations/` terpisah dari `database/models/` agar histori perubahan skema dapat dilacak independen dari definisi model saat ini.
- `deployment/env/` menyimpan template konfigurasi koneksi database (PostgreSQL/MySQL) sehingga perpindahan database cukup mengganti file environment, bukan kode.

---

# TAHAP 10 — SOFTWARE DEVELOPMENT FLOW

```mermaid
flowchart TD
    A[Requirement] --> B[Architecture]
    B --> C[Database Design]
    C --> D[API Design]
    D --> E[Backend Development]
    E --> F[Frontend Development]
    F --> G[AI Model Integration]
    G --> H[ESP32 Integration]
    H --> I[Deployment]
    I --> J[Testing]
    J --> K[Maintenance]

    K -.feedback loop.-> B
```

| Tahap | Deliverable Utama |
|---|---|
| Requirement | SRS (sudah selesai pada dokumen sebelumnya) |
| Architecture | Dokumen ini — context, use case, activity, sequence, component, deployment, clean architecture |
| Database Design | ERD, skema tabel, migration plan |
| API Design | OpenAPI spec untuk seluruh endpoint (login, upload, predict, history, statistics, status, latest, download) |
| Backend Development | Implementasi Service & Repository Layer sesuai Clean Architecture |
| Frontend Development | Implementasi Dashboard, History, Statistics, Monitoring sesuai komponen NextJS |
| AI Model Integration | Integrasi MobileNetV2 FOMO TFLite ke Inference Engine |
| ESP32 Integration | Implementasi firmware komunikasi HTTP POST dari sisi hardware |
| Deployment | Setup VPS, Nginx, systemd service, SSL |
| Testing | Unit test (service layer), integration test (API), end-to-end test (ESP32 → Dashboard) |
| Maintenance | Monitoring, backup berkala, update model, bug fixing |

---

*Dokumen ini merupakan hasil Tahap Desain Arsitektur. Implementasi (source code) belum dimulai sesuai instruksi.*
